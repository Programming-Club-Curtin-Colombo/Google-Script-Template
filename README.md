This repository is NOT a Node.js application.

It is a Google Apps Script project.

Node.js is only used for:

- linting
- formatting
- optional deployment tooling

The actual runtime is Google Apps Script.

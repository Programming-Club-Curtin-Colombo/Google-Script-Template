/**
 * Utilities Layer
 */
const Utils = (() => {
  /**
   * Identity function.
   * @param {*} value
   * @return {*}
   */
  function identity(value) {
    return value;
  }

  return {
    identity,
  };
})();
